/*
Write a programme that convert ASCII codes to ASCII character.
*/
#include<stdio.h>
int main()
{
    int n,o;
    printf("Enter two ASCII codes:");
    scanf("%d %d",&n,&o);
    printf("The ASCII characters are: %c %c",n,o);
    return 0;
}
